{
  "name": "baoba-tickets-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . baoba-tickets-app --platform=win32 --arch=x64 --out=dist --overwrite",
    "build-mac": "electron-packager . baoba-tickets-app --platform=darwin --arch=x64 --out=dist --overwrite",
    "build-linux": "electron-packager . baoba-tickets-app --platform=linux --arch=x64 --out=dist --overwrite",
    "package": "electron-packager . baoba-tickets-app --platform=all --arch=x64 --out=dist --overwrite",
    "postinstall": "electron-rebuild",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^36.3.2",
    "electron-reload": "^2.0.0-alpha.1"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
